# Auto detect text files and perform LF normalization
* text=auto

# Notebooks: keep diffs readable and avoid CRLF churn
*.ipynb text eol=lf

# Explicit binary types
*.png binary
*.jpg binary
*.pdf binary
